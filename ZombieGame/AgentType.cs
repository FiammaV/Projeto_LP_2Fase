namespace ZombieGame {
    /// <summary>
    /// Enumeration of agents
    /// </summary>
    public enum AgentType {
        // There will be two types of agent
        Human,
        Zombie
    }
}
